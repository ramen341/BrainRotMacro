import time
import pyautogui
import keyboard

while True:
    if keyboard.is_pressed('p'):
        x, y = pyautogui.position()
        print(f"Current mouse position: ({x}, {y})")
        time.sleep(1)  # Add a small delay to avoid multiple prints